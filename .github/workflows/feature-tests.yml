name: Feature Tests

on:
  push:
    branches:
      - 'feature/**'

  jobs:
    test:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup nodejs
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

#      - name: Run unit Tests
#        run: npm run test
